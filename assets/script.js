var formEl = document.querySelector("#main-form");
var numOneInput = document.querySelector("#number-list");
var numTwoInput = document.querySelector("#number-input");
var resultsContainer = document.querySelector("#table-container");

var formSubmitHandler = function (event) {
  event.preventDefault();

  // check to see if results container already has results and clear
  // get value for number one input
  // get value for number two input
  // create an array made up of number one input + 9 continued until it reaches number two input
  // add together every digit in array
};

formEl.addEventListener("submit", formSubmitHandler);
